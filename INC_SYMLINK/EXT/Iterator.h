#pragma once

enum Operator
    {
    ADD,
    MULTIPLY
    };

/*----------------------------------------------------------------------*\
 |*			Class	 					*|
 \*---------------------------------------------------------------------*/

/**
 * Iterator iteratorXXX(0,10,1,Operator::ADD);
 *
 * for (int xxx = iteratorXXX.min; xxx <= iteratorXXX.max; xxx = iteratorXXX.inc()
 */
struct Iterator
    {
	/*------------------------------------------------------------*\
        |*			Constructors 			      *|
	 \*------------------------------------------------------------*/

    public:

	Iterator(int min , int max , int step , Operator op);

	/*------------------------------------------------------------*\
        |*			Methodes 			      *|
	 \*------------------------------------------------------------*/

    public:

	int inc();

	/**
	 * [min,max]
	 */
	int getSize();

	void clear();



	/*------------------------------------------------------------*\
        |*			Attributs			      *|
	 \*------------------------------------------------------------*/

    public:

	// Input/Outpus
	int min;
	int max;

    private:

	int step;
	Operator op;
	int size;

	// Tools
	int i;
    };

/*----------------------------------------------------------------------*\
 |*			End	 					*|
 \*---------------------------------------------------------------------*/
