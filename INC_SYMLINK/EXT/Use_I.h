#pragma once

#include "RunnableGPU_I.h"
#include "Hardware.h"
#include "Grid.h"

/*----------------------------------------------------------------------*\
 |*			Class	 					*|
 \*---------------------------------------------------------------------*/

class Use_I
    {
	/*------------------------------------------------------------*\
        |*			Constructors 			      *|
	 \*------------------------------------------------------------*/

    public:

	virtual ~Use_I()
	    {

	    }

	/*------------------------------------------------------------*\
        |*			Methodes 			      *|
	 \*------------------------------------------------------------*/

    public:

	virtual RunnableGPU_I* getRunnableGPU()=0;

	virtual bool isOk(bool isVerbose = false)=0;

	/**
	 * true iff to be keeped
	 */
	virtual bool filter(const Grid& grid);

	/*------------------------------------------------------------*\
	        |*			Methodes 			      *|
	 \*------------------------------------------------------------*/

    protected:

	const int MP = Hardware::getMPCount();
	const int CORE_MP = Hardware::getCoreCountMP();

    };

/*----------------------------------------------------------------------*\
 |*			End	 					*|
 \*---------------------------------------------------------------------*/
